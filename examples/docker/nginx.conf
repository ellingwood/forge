server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Simulate S3 + CloudFront behavior with clean URL support.
    # try_files resolves /blog/my-post/ and /blog/my-post to dir/index.html.
    location / {
        try_files $uri $uri/index.html =404;
    }
}
